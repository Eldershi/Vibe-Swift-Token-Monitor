{
  "name": "token-monitor-hub-quota-cycles",
  "version": "0.1.0-experiment.1",
  "private": true,
  "type": "module",
  "scripts": { "test": "node --test tests/*.test.js" },
  "engines": { "node": ">=22" }
}
