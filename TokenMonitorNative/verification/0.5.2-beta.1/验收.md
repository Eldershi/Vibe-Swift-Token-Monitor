# 0.5.2-beta.1（34）开发与验收

日期：2026-09-15。基于正式版 0.5.1（33）及工作区已有未提交修改继续开发。未安装替换、未推送、未发布 Release。

## 交付

- 展示版本 `0.5.2-beta.1`，系统数字版本 `0.5.2`，构建 `34`。
- Bundle ID `local.tokenmonitor.native.beta`、数据目录、LaunchAgent 和旧 0.4.1 构建入口保持不变。
- 独立包：`dist/Token Monitor Native Beta.app`；附件：`Token-Monitor-Native-0.5.2-beta.1-34-arm64.zip`，52,117,895 字节。
- ZIP SHA-256：`9ca8db451236c193375a574b7bcfc4d065f48c97b0cd48367e6df77f545d55d7`。
- 安装脚本按包元数据生成 `Token Monitor Native Beta 0.5.2-beta.1 (34).app`；本次未执行安装脚本。

## 修改

- 柱状图命中共用实际绘制的胶囊路径，排除柱顶空白、柱间间隙、坐标轴、零值及缺失日期。保留 AX 数据、柱顶居中提示与主题色内描边。
- 四个浮动按钮各自带鼠标透明遮挡标记，共用窗口坐标清除下方热力图/柱状图悬停，不修改按钮事件。命中同时检查自身 bounds 和 visibleRect；后者在 AppKit 下可能超出自身 bounds，单独使用会错误扩大遮挡。
- 工具菜单每次打开从当前偏好生成 NSMenuItem.state，包括全部工具；动作继续使用同一数据来源偏好。
- 两个首页栏目保持 activity/trends ID、独立显隐和排序；详情入口统一到活动。旧“趋势”和“活动”页面值均映射到活动；页面菜单保留五项，只有一个 Activity。
- 活动详情按热力图 / Heatmap、趋势 / Trends 排列并各有标题，保留历史明细及两个图表的原生滚动实例和 AX 描述。普通更新没有新增重建身份或重置偏移机制。
- 布局按 Home Sections / 首页栏目、Quota / 额度、Devices / 设备分组；额度、设备控件写明首页作用范围，恢复默认独立位于所有组之后。不新增详情占位控件。schema 3、默认值、保存观察和恢复逻辑不变。

## 已通过

运行环境：Apple Silicon，macOS 27.0（26A428），Xcode beta / macOS 27 SDK。以下为本次实际结果，不引用旧版本验收代替。

| 检查 | 结果 |
| --- | --- |
| Swift 全量回归 | 91 项、0 失败 |
| 后台 Node 回归 | 31 项、0 失败 |
| 本地化静态检查 | 269 个中英键，覆盖、占位符和隐私说明通过 |
| 最终包中英文资源加载 | en、zh-Hans 两个运行时验证通过 |
| 独立 release 构建 | 临时 scratch-path 编译、资源打包通过 |
| 严格签名及资源冒烟 | build-beta 中 codesign --verify --deep --strict、应用图标及后台运行时自检通过 |
| 最终包合成日志 | 真实解析器、token 分区、费用、重启、月份边界、日历史、追加/截断/轮换、零值、来源只读通过 |
| 公开检查 | staged 与全历史/工作区检查通过；未执行推送 |
| 差异格式 | git diff --check 通过 |

新增自动回归包括：柱形顶部和圆角边界、零值/缺失与间隙排除；两种图表在直接容器及嵌套滚动容器下的遮挡清除、移开/隐藏标记恢复、原生按钮 hitTest 不受标记影响；原生菜单动作、重开后唯一勾选、全部工具、数据值及偏好编码恢复；旧页面映射和两个首页入口；两栏目四种显隐组合及两种顺序的真实临时磁盘往返。原有原生滚动偏移、刷新、外观传播、AX 零值数据回归也在全量套件内通过。

独立应用预览仅加载合成报告，未连接真实后台：

- 320 pt 英文深色、中文浅色活动页实际截图检查：两个图表上下分组、轴与月份、底部玻璃控件可见，无新增横向溢出；AX 树保留热力图、趋势与零值/缺失日期数据。
- 使用旧“趋势”启动参数进入中文“活动”页；随后无页面覆盖参数启动，分别点击首页热力图、每日趋势标题，均进入同一 Activity 页，返回正常。
- 页面菜单实际包含 Overview / Devices / Models / Quota / Activity，没有独立 Trends。
- 工具菜单显示全部工具、Claude、Codex；实际选择全部工具后来源标签从 Codex 更新为全部工具，示例日值从 4,000 更新为 5,000。勾选值由上述原生菜单自动回归验证，未将 AX 的菜单高亮误记为勾选截图验收。
- 点击浮动设置按钮正常打开设置；中英布局均确认首页栏目、额度、设备三个标题独立，恢复默认在组外。此前已验证独立关闭热力图、通过原生无障碍“上移”交换趋势顺序。

## 本轮修订：分组、图标与可见高亮

- 首页热力图为 calendar，趋势为 chart.xyaxis.line；与共同详情目标独立。实际英文首页截图和 AX 树均确认不同图标与 Heatmap / Trends 标题。活动详情左下按钮与页面菜单最终采用 waveform.path.ecg.text.clipboard。前一轮中英文预览检查的是此前的填充剪贴板变体；本次新符号可用性检查、4 项活动导航回归、独立包重建、严格签名及应用/后台资源冒烟通过；未重跑此前全量矩阵。
- 高亮改为浅色主题色向黑混合 30%、深色向白混合 45%，在 sRGB 分量上直接混合，保留主题色且增加与实心柱的差异。覆盖层显式启用 layer backing，绘制顺序保持在 Canvas 宿主上方。
- 栅格回归发现极短柱在内缩后形成零高度路径，导致描边不绘制。修复为在原胶囊内填充轮廓色；普通柱内缩半线宽且同步缩小半径，全部高亮裁剪在实际柱形内。
- 新增两项测试：明暗轮廓颜色值；真实 FixedBarChart 宿主渲染前后图像不同、原生覆盖层在最上方、普通/短/极短柱均有可见高亮像素且不超出柱形边界。与原有命中、圆角、零值/缺失、菜单、偏好、滚动和遮挡测试一起，91 项通过。
- 程序化图表栅格产物保留在 rendered-bars：例如[浅色普通柱](rendered-bars/bar-NSAppearanceNameAqua-1.png)、[深色普通柱](rendered-bars/bar-NSAppearanceNameDarkAqua-1.png)、[浅色极短柱](rendered-bars/bar-NSAppearanceNameAqua-3.png)。图像使用透明背景，是原生测试宿主产物，不冒充物理鼠标实机截图。
- 最终包 320 pt 英文浅色首页：点击定位指针于柱形，提示与深色内轮廓可见；同一 X 的柱顶空白清除提示。将指针移到柱形下方的玻璃 Agent 按钮，菜单正常打开，关闭菜单后无图表穿透提示。
- 最终包 320 pt 英文浅色、中文深色活动详情：实际截图确认 Trends / 趋势、Heatmap / 热力图，柱形内轮廓及柱顶提示可见，深色轮廓明度增加。中文和英文布局 AX 树都确认 Devices / 设备是独立标题。
- 本轮重跑独立构建、严格签名、应用/后台资源冒烟、31 项后台回归、269 个本地化键与最终包合成日志解析回归，均通过。版本仍为 0.5.2-beta.1（34）。

## 未测与限制

- 已做点击定位指针的实际高亮/空白/首页玻璃按钮检查；未完成物理鼠标连续移动跨边缘及四种首页/详情图表的完整悬停人工矩阵，不能宣称全部真实鼠标场景验收完成。
- 未完成实际触控板历史浏览、拖动排序手柄、滚动条“始终显示”的系统配置矩阵；偏移与顺序持久化有自动回归，预览使用临时偏好，不将预览重开视为用户配置磁盘恢复验收。
- 中英文和浅深色各有实际界面检查，但未覆盖全部语言×主题×页面组合；未做完整 VoiceOver、macOS 26 真机、长期采样/资源占用、注销登录及后台生命周期操作。
- SwiftPM native driver 和项目原有 AppKit/Keychain 弃用警告仍在；构建冒烟输出一条 sandbox_extension_issue_file_to_process 提示，但进程退出成功、资源自检及严格签名验证均通过。

## 复现命令

从仓库根目录运行；编译缓存、GUI 测试及临时 HTTP 端口需要相应沙箱权限。

```sh
swift test --build-system native --package-path TokenMonitorNative
TokenMonitorNative/Backend/runtime/node --test TokenMonitorNative/Backend/tests/*.test.cjs
python3 TokenMonitorNative/scripts/check-localization.py
bash TokenMonitorNative/scripts/build-beta.sh
python3 TokenMonitorNative/scripts/test-beta-logs.py --app 'TokenMonitorNative/dist/Token Monitor Native Beta.app'
'TokenMonitorNative/dist/Token Monitor Native Beta.app/Contents/MacOS/TokenMonitorNative' --verify-localization en -AppleLanguages '(en)'
'TokenMonitorNative/dist/Token Monitor Native Beta.app/Contents/MacOS/TokenMonitorNative' --verify-localization zh-Hans -AppleLanguages '(zh-Hans)'
python3 scripts/check-publication.py --staged
python3 scripts/check-publication.py
git diff --check
```

## 时间跨度对照入口

新增独立 A/B/C 诊断入口：共用 NSToolbarItemGroup，A 纯色正文、B 完整正文冻结、C 正常周期更新；关闭定时更新和真实网络，页面使用内存状态，窗口不保存尺寸。日志记录选中事件、控件/工具栏实例一致性及窗口激活/外观。正式入口不安装诊断处理器，未改选中动画或控件颜色。

91 项 Swift 回归、独立构建、严格签名与资源冒烟通过。已用 AX 点击在浅色 A、B 中各执行五轮“今天→总计→本月”；随后用户接手实际动画观察。AX 操作不等于物理悬停；本轮未完成深色、快速点击及键盘矩阵，也未获得足以判断短暂明度闪动的连续画面。用户随后反馈：“A 也闪，都一样明显”，并认为是动画叠加。该观察说明无需正文刷新即可复现，支持原生悬停反馈与选中滑移动画衔接的解释；未通过逐帧证据确认内部合成机制，也不称为苹果已确认的 Bug。上述为初次诊断；用户随后要求验证公开 API 取消横移，接受更朴素的原生样式。

### 取消横移候选（待用户确认）

仅隔离入口新增 macOS 27 `NSToolbarItemGroup.role = .valueSelection`，保留 selectOne；⌘4 automatic / ⌘5 valueSelection 共用同一实例，日志记录 role。正式入口尚未采用。本轮 91 项 Swift 回归、独立构建、严格签名和应用/后台资源冒烟通过；横移、明度衔接、浅深模式各五轮真实鼠标/键盘矩阵待用户检查，不能标为已修复。若仍滑动，下一步测试 rounded NSSegmentedControl。

用户反馈第一候选 valueSelection 的“明度衔接没有改善”；未明确反馈横移结果，不推断动画已消失。第二候选 `--period-rounded` 仅在隔离入口使用工具栏持有的原生 NSSegmentedControl（rounded/selectOne，macOS 27 valueSelection），不额外叠加玻璃。本轮 91 项 Swift 回归、独立构建、严格签名及应用/后台资源冒烟通过；第二候选连续动画与完整五轮交互矩阵待用户检查，正式入口保持原实现。

## GitHub 更新功能

- 设置/关于增加软件更新组：手动检查、自动检查开关（schema 3 新字段默认 false）、发现版本、下载并安装及 GitHub 版本页入口。自动检查为运行时每 6 小时及启动一次，安装始终确认；仅正式 Release，不降级较新 Beta。
- 使用固定 Sparkle 2.10.0，框架与 XPC/Updater 辅助程序随包签名；更新清单与归档均使用固定 Ed25519 身份签名，校验后解压。保留原 Bundle ID、数据目录、后台用户启用偏好，更新沿用异步退出保存并注销旧后台。
- 自动和手动检查共用 GitHubReleaseClient，不附带 Cookie、账号凭据或采集数据。预览/冒烟/隔离验证不启动自动检查或安装。
- Swift 全量 95 项通过；新增正式/Beta/草稿/非法版本比较、跨仓库/版本清单拒绝、自动检查默认及磁盘恢复、200/403/404/429/500 HTTP 分支与无凭据请求验证。后台 31 项通过，本地化增加至 290 个键，独立构建、严格签名、资源冒烟和最终包合成解析器回归通过。
- 实际 GitHub 只读查询：latest=v0.5.1，newer=false，signed-feed=false。当前正式 Release 未提供 appcast.xml；本地生成的新版 appcast.xml 及 ZIP 尚未发布，因此未做线上下载安装。
- 签名私钥只保存在 .local-private/update-signing/ed25519.seed（0600、Git 忽略），没有打包或提交；公钥在 Resources/UpdatePublicKey.txt。构建产物 appcast.xml 及 ZIP 已用官方工具验证签名。发布说明见 ../../UPDATES.md。
- 未测：真实安装副本的升级、系统权限提示、失败回退、后台服务注销/恢复端到端流程；后台实际 6 小时等待、长时间断网恢复，以及 macOS 26 更新安装。未为本次功能安装替换、推送或发布 Release。

新增只读运行验证：

```sh
'TokenMonitorNative/dist/Token Monitor Native Beta.app/Contents/MacOS/TokenMonitorNative' --verify-update-check
```

补充验证：最终包 Sparkle 配置自检通过（签名清单/归档验证、自动下载关闭），GitHub 实际查询通过；篡改归档的官方签名验证被拒绝。一次全量回归中既有 StateTests.testInvalidNewSnapshotPreservesLastGoodState 出现 URLSession 已失效异常并中断，未修改相关采集逻辑；复跑全量 95 项通过，此间歇性会话问题保留为未解决项。设置新组未做完整 GUI 矩阵，后续随真实安装流程一起验收。

### 更新设置精简

删除说明与上次检查时间行，将检查按钮和 GitHub 版本入口放在末行；状态与可用更新的安装操作保留在上方。本轮 290 个本地化键检查、独立构建、签名及应用/后台资源冒烟通过，重新生成签名清单和 ZIP。此轮仅调整布局，未重跑 Swift/后台功能回归，未做新增 GUI 交互矩阵；未安装替换、推送或发布。

## 活动明细年/月展开

- 保留两张图表，将平铺日列表替换为原生 DisclosureGroup；年、月、日倒序，年月显示来自日值的小计；缺失值不作零，部分记录标记“已记录”。会话状态按来源地址/Agent 分离，独立年份/月标识避免一月冲突，普通刷新不自动打开新增月份。
- 自动验证：99 项 Swift 全量通过，新增跨年、闰日、倒序、前置补白裁剪、零/缺失/非法值、年月汇总、来源/Agent 隔离、刷新及默认展开初始化测试；292 个双语键检查通过。最终独立构建、严格签名、应用/后台资源冒烟、更新清单/归档签名及最终包合成解析器回归通过。此次没有修改后台，未重跑 Node 全量回归。
- 实际交互：合成三年历史预览，320 pt 英文浅色检查展开与日值；随后最终缩进版本的320 pt中文深色截图检查年/月/日层级、数值不截断。原生 AX 明确显示展开状态及完整每日日期；月份行空白可收起，九月/八月可同时展开。从活动返回首页再经趋势入口返回，原展开九月保持；重启预览月份恢复收起。
- 未测：完整键盘焦点与 VoiceOver 操作（一次 Tab/空格未确认焦点，不计通过）、真实数据自动刷新交互、图表横向历史偏移与展开组合矩阵、所有中英文×主题组合、长时间滚动性能及macOS 26真机。99项自动回归覆盖既有历史偏移逻辑，不等同此次完整人工滚动验收。
- 版本仍为0.5.2-beta.1（34）；测试包及签名清单仅本地重建，未安装替换、推送或发布。

### 顶部透明滚动

正文滚动视口延伸到透明标题栏后方，取消整条顶部遮挡与重复安全区内边距；顶部工具栏样式保留，原生控件自身范围屏蔽图表悬停。100项Swift通过，包括新增顶部NSControl边界/隐藏状态与无重复内边距检查。中文深色320 pt实际滚动截图已确认热力图透过顶部玻璃、间隙直接显示内容；最终再次收紧初始留白后独立构建、签名、资源冒烟及签名更新清单生成通过。用户正在操作预览设置页，因此未再次重启窗口复查最后一次留白微调；浅色顶部与真实连续指针遮挡矩阵未测。没有安装替换、推送或发布。

## Icon Composer 应用图标

用户提供的 Token Monitor.icon 原样纳入 Resources，文档和3个SVG内容哈希与源一致。通过 Apple actool 编译到主包，包含 Assets.car、Token Monitor.icns，以及工具生成的 CFBundleIconName/CFBundleIconFile；界面符号 bundle 保持独立。两个打包入口共用编译脚本，图标生成在最终签名前完成。

实际通过：Apple编译成功；assetutil确认浅色/深色/着色IconImageStack；ICNS转换预览外观检查；最终主包元数据、图标资源及应用身份检查；独立Beta构建、292键本地化、应用/后台资源冒烟、暂存应用严格签名、ZIP及appcast签名验证。dist副本复查发现Finder扩展属性污染，清除该构建副本扩展属性后重新验证。未重跑Swift功能回归；未安装验证Finder/Dock缓存刷新、真实系统着色切换或旧0.4.1构建入口。继续0.5.2-beta.1（34），未安装替换、推送或发布。

补充：dist应用清理扩展属性后严格签名通过；交付ZIP独立解压后严格签名也通过，归档内容无该污染。
