# Codex 优先开发路线图

更新：2026-09-14。**本文是计划，不是已实现功能清单。** 按验收结果推进，暂不承诺发布日期；本次只更新公开说明与问题分析，不修改应用代码、版本标记、标签或 Release 附件。

## 定位与基线

当前 **0.5.2（构建 35）主要针对 Codex，仅完成基础功能验证，尚不适合普通用户直接下载安装使用**。菜单栏自定义等体验未完善；Claude Code 等现有实验适配不代表正式支持，近期不扩展其他 AI 工具。

项目独立维护，复用原版的部分 MIT 后台模块，非 OpenAI 或原版作者的官方客户端；详见[项目关系](README.md#与原版项目的关系)。当前包与源码规模、已确认技术债见[现有问题](docs/CURRENT_ISSUES.md)。不全盘重写，也不承诺仅靠整理源码显著减小安装包。

推进顺序：**可靠采样 → 额度换算与重置预报 → 展示完善**。分层应用图标已在 0.5.2 接入。

## 第一阶段：后台可靠性与重置采样（计划）

- 分离采集调度、持久化、Hub 同步和预估计算；高频写盘串行异步化，合并重复保存，正常退出完成末次保存。
- 完善停止、重启、断网恢复：处理在途请求、待上传状态、失败重试及慢速 SSE 客户端；不破坏同一设备单一上传者、历史基线和绝对快照幂等性。
- 历史合并分别定义累计计数、费用、派生指标与元数据规则；统一版本来源，修正上传记录残留的旧 beta 版本。
- 根据实际调用关系收敛未使用的上游模块与依赖，保留固定来源、补丁记录及许可证；不能仅凭文件名删除依赖。
- 新增版本化的本地采样和重置事件存储：账号匿名标识、套餐/额度桶、窗口时长、官方重置时间、已用比例、Token 增量、采样时间及完整度。账号标识用于隔离样本，不记录凭据或会话正文。

**重置是独立事件。** 区分正常到期、提前重置、额度回补、时间调整、账号/额度桶切换及疑似服务器回滚。一次百分比下降不能判定官方重置；结合连续新鲜观测确认，无法解释的变化保留为未知。事件前后划分采样区段，保留原始观测及修正记录；额度恢复不算负消耗，不删除历史。

现有代码已在官方重置时间附近刷新。本阶段新增事件识别与历史记录，不重复建设倒计时，也不因全局公告直接认定本账号到账。

## 第二阶段：额度换算与重置预报（计划）

### Token / 额度换算

同时支持软件预估、用户自定义公式和结果对照。

- 从可解释的分模型加权基线开始，按个人历史校准；账号、套餐与额度窗口分开处理，避免 Token 分类重复计数。
- 自定义公式支持参数、保存与样本预览；只解释受限数学表达式，不执行任意代码。无法可靠反推时只提供正向换算。
- 官方余额与预估分开；剩余 Token 预估注明模型或使用组合，并给出经验范围。缺数据、共享额度/其他设备消耗无法解释、规则变化时降级，不强行显示精确值。
- 目标为**满周额度误差不超过 5 个百分点**，尚未验证达成。训练与验证按时间分离，至少覆盖两个完整独立验证周，并报告日内偏差。
- 正常完整周与中断周期分别报告。提前重置或回滚不能用来清零误差；重置附近不可靠样本不参与拟合，但保留在误差和覆盖率记录中。

### 不规则重置预报

参考 [Codex Reset Radar](https://github.com/whmc76/codex-reset-radar) 的公开信号处理思路。该项目提供未来 72 小时的启发式预报，其评分**不是经统计校准的概率，也不是官方承诺**。本项目当前没有实现该能力；如直接复用第三方代码，先检查许可证并固定版本，没有明确许可时只参考公开思路。

额度详情中分别呈现：

| 信息 | 依据与显示 |
|---|---|
| 当前账号重置时间 | 最新有效额度接口返回值，附更新时间 |
| 额外重置预报 | 官方公告或公开信号推导的时间范围，附来源和证据强度 |
| 本账号已确认恢复 | 连续实际观测确认，不能仅凭全局公告认定到账 |

来源、时效与通知规则：

- **官方优先、社区可选。** 默认读取官方状态与可公开访问的官方消息；社区聚合、历史规律和用户报告需主动开启。不绕过登录限制，不读取浏览器凭据。
- 保存原始出处、发布时间、事件时间和适用范围；转发或多个聚合站引用同一消息只算一份证据。官方故障不等于补偿重置，个人恢复不等于全局重置。
- 预报未来 72 小时；时间线索足够时按 6 小时区间显示，否则显示“时间未明确”。先用证据等级，不伪装成精确概率。
- 没有有效线索显示“暂无可靠预报”；来源不可达显示不可用或过期，不能解释为“不会重置”。官方撤回、消息过期或时间变化时修正预报。
- 系统通知默认关闭；开启后去重，仅通知明确预告、重大修正或本账号确认恢复。
- 只放在额度详情，不增加导航；后续菜单栏和 Widget 复用同一预报快照。
- **预测重置不计入实际可用额度，不自动修改消耗预算，不执行购买或兑换重置。**

## 后续展示与可选功能（尚未实现或未完善）

| 阶段 | 交付范围 |
|---|---|
| 视觉完善 | 0.5.2 已接入 Icon Composer 分层图标；继续验证不同系统外观与辅助功能 |
| 饼图 / 环形图 | Codex 模型 Token / 费用占比，沿用统计周期，保留明细、未知项和无障碍信息 |
| 菜单栏 | 自定义内置小图标、显示方式及用量/额度指标，布局宽度稳定 |
| Mac Widget | 小号、中号只读组件，共享脱敏快照并显示更新时间；不承诺秒级刷新 |
| 可选增强 | 默认关闭的额度低位/异常消耗提醒；本地统计与预估结果导出 |
| iOS | Mac 版稳定后评估需求，优先考虑只读 Hub 配套客户端，不承诺启动开发 |

新增版本化的本地采样、重置事件、预估与预报快照接口，以及公式和来源偏好。共享 Hub 协议保持兼容；Widget 不持有凭据、不运行采集器，iOS 不进入近期实现范围。

## 验收与公开使用门槛

保留现有 Swift 与后台回归（最新数量见对应版本发布记录），再增加以下验收；现有通过记录不能证明新算法或预报有效。

| 类别 | 必须覆盖 |
|---|---|
| 重置 | 正常到期、提前全量/部分恢复、重置时间前移后回滚、账号/桶切换、接口取整、乱序缓存 |
| 预报 | 旧消息再传播、时区/夏令时、模糊时间、撤回、源站故障、重复引用、适用账号不匹配 |
| 换算 | 跨重置分段、遗漏用量、自定义公式异常、独立周回放、规则变化后的降级 |
| 后台 | 在途请求停止、重启去重、断网恢复、写入顺序、慢速 SSE 客户端、长期资源占用 |

预报单独报告命中、误报、漏报、提前量及时间区间误差，**不套用额度换算的 5 个百分点目标**。历史消息按当时可见时间回放，禁止用事后确认消息“预测过去”；未经验证的评分始终标为实验。

面向普通用户推荐下载前，还须完成 macOS 26 真机、长期运行、登录恢复、辅助功能、安装升级回退及签名公证验证。阶段完成以这些证据为准，不以 README 宣布或单元测试通过代替实际验收。
